<?xml version="1.0" encoding="utf-8"?>
<RunSettings>
  <RunConfiguration>
    <!-- No TestSessionTimeout. Zombie process cleanup handled by scripts/test.sh
         via timeout and TRX logger. Setting TestSessionTimeout here kills the
         test host mid-run and reports passing tests as failures. -->
  </RunConfiguration>
</RunSettings>
